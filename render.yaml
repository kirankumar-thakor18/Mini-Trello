services:
  - type: web
    name: mini-trello
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/tasks
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: Mini-Trello